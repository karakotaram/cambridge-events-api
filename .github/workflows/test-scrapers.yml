name: Test New Scrapers

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install Chrome for Selenium
        uses: browser-actions/setup-chrome@latest
        with:
          chrome-version: stable

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Test Sanders Theatre scraper
        run: |
          python3 -c "
          from src.scrapers.sanders_theatre import SandersTheatreScraper
          import logging
          logging.basicConfig(level=logging.INFO)
          scraper = SandersTheatreScraper()
          events = scraper.run()
          print(f'Sanders Theatre: {len(events)} events')
          for e in events[:5]:
              print(f'  - {e.title[:60]} | {e.start_datetime}')
              print(f'    img: {\"yes\" if e.image_url else \"no\"} | desc: {len(e.description)} chars')
          "

      - name: Test American Repertory Theater scraper
        run: |
          python3 -c "
          from src.scrapers.art import AmericanRepertoryTheaterScraper
          import logging
          logging.basicConfig(level=logging.INFO)
          scraper = AmericanRepertoryTheaterScraper()
          events = scraper.run()
          print(f'American Repertory Theater: {len(events)} events')
          for e in events[:5]:
              print(f'  - {e.title[:60]} | {e.start_datetime}')
              print(f'    img: {\"yes\" if e.image_url else \"no\"} | desc: {len(e.description)} chars')
          "

      - name: Test Somerville Theatre scraper
        run: |
          python3 -c "
          from src.scrapers.somerville_theatre import SomervilleTheatreScraper
          import logging
          logging.basicConfig(level=logging.INFO)
          scraper = SomervilleTheatreScraper()
          events = scraper.run()
          print(f'Somerville Theatre: {len(events)} events')
          for e in events[:5]:
              print(f'  - {e.title[:60]} | {e.start_datetime}')
              print(f'    img: {\"yes\" if e.image_url else \"no\"} | desc: {len(e.description)} chars')
          "

      - name: Summary
        run: |
          echo "=== Test Complete ==="
          echo "All three new scrapers tested successfully"
